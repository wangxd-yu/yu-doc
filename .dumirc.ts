import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/yu-doc',
  publicPath: '/yu-doc/',
  favicons: ['/yu-doc/img/favicon.ico'],
  themeConfig: {
    name: 'yu-doc',
  },
});
